import React from 'react';

const styles = {
  searchStyle: {
    textAlign: 'center',
    marginTop: '20%',
  },
};
const Loader = () => <div style={styles.searchStyle}>Searching ...</div>;
export default Loader;
